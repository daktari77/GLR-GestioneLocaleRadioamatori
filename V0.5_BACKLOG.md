# GestioneSoci v0.5 — Backlog (stabilità + robustezza)

**Obiettivo**: rilasciare v0.5 entro fine gennaio con focus su affidabilità, regressioni ridotte e workflow CD/Documenti più completi.

## Fonte punti
- `tests/to-do` (nota locale)
- TODO nei changelog e note tecniche nel repo

## Priorità P0 (must-have per “stabile e robusta”)
1. **Calendario: reminder più affidabili**
   - Cosa: migliorare gestione reminder (creazione, modifica, scadenze, edge-case date).
   - Done when: reminder salvati/aggiornati senza errori e visibili coerentemente in UI; nessun crash su date mancanti/errate.

2. **Consiglio Direttivo: Verbali — elenco + export**
   - Cosa: generare elenco verbali; esportare elenco in Excel con (data, numero CD, ordine del giorno).
   - Done when: export crea un `.xlsx` valido con colonne richieste; righe ordinate per data/numero; gestisce campi vuoti.

3. **Consiglio Direttivo: Delibere — “Libro delibere”**
   - Cosa: vista/registro delibere (libro) e base dati coerente.
   - Done when: elenco delibere filtrabile/ordinabile almeno per data/numero; nessun dato perso tra DB e documentale.

4. **Hardening DB e gestione errori**
   - Cosa: audit rapido dei punti dove possono emergere `sqlite3.OperationalError: database is locked` e input invalidi; uniformare mapping errori (`map_sqlite_exception`).
   - Done when: i path critici fanno retry o mostrano errore chiaro; nessun traceback “raw” in UI.

## Priorità P1 (should-have)
5. **Statistiche: contatori documenti**
   - Cosa: aggiungere in Statistiche:
     - numero documenti socio
     - numero documenti sezione
   - Done when: contatori corretti e calcolati rapidamente anche con dataset medio (senza bloccare UI a lungo).

6. **Delibere/Verbali: rivedere tab UI (semplificazione o integrazione)**
   - Cosa: decidere e implementare una delle due:
     - rimuovere tab Delibere/Verbali, oppure
     - integrarli con elenco documenti (collegamento chiaro tra record e file).
   - Done when: un solo percorso UX “ovvio” per trovare documenti legati a delibere/verbali.

7. **Delibere: import da documenti (opzionale, se valore alto)**
   - Cosa: “importa da doc?” per popolare metadati delibera (anche solo assistito).
   - Done when: riduce data-entry manuale senza introdurre fragilità.

## Priorità P2 (nice-to-have / se rimane tempo)
8. **Test: integrazione CD e documentale**
   - Cosa: aggiungere test mirati su workflow elenco+export e collegamenti documenti.

9. **Test/qualità: coverage e scenari dataset grandi**
   - Cosa: misurare copertura (minimo) e aggiungere 1–2 test di performance “light” (10k soci simulati) per query principali.

## Note operative (proposta di ordine di lavoro)
1) Verbali (elenco + export Excel) → 2) Libro delibere → 3) Calendario/reminder → 4) Statistiche contatori → 5) scelta UX tab
